apiVersion: apps/v1
kind: Deployment
metadata:
  name: ckan-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ckan-redis
  template:
    metadata:
      labels:
        app: ckan-redis
    spec:
      containers:
        - name: redis
          image: redis:6
          args:
            - redis-server
            - --save ""
            - --appendonly no
            - --bind 0.0.0.0
            - --protected-mode no
          ports:
            - containerPort: 6379

---

apiVersion: v1
kind: Service
metadata:
  name: ckan-redis
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: ckan-redis